
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Be My Valentine, Simran?</title>
  <style>
    body {
      background: #f5f5f5;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      font-family: Arial, sans-serif;
    }

    .browser {
      width: 420px;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 15px 40px rgba(0,0,0,0.2);
      background: white;
      position: relative;
    }

    .browser-top {
      background: #eaeaea;
      padding: 8px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
    }

    .red { background: #ff5f56; }
    .yellow { background: #ffbd2e; }
    .green { background: #27c93f; }

    .address-bar {
      margin-left: 10px;
      background: white;
      padding: 4px 8px;
      border-radius: 6px;
      font-size: 12px;
      color: #555;
      flex-grow: 1;
    }

    .content {
      padding: 30px;
      text-align: center;
    }

    h1 {
      color: #ff4d6d;
    }

    button {
      margin: 10px;
      padding: 10px 20px;
      font-size: 16px;
      border-radius: 20px;
      border: none;
      cursor: pointer;
      position: relative;
    }

    .yes {
      background: #ff4d6d;
      color: white;
    }

    .no {
      background: #ccc;
      position: absolute;
    }
  </style>
</head>
<body>
  <div class="browser">
    <div class="browser-top">
      <div class="dot red"></div>
      <div class="dot yellow"></div>
      <div class="dot green"></div>
      <div class="address-bar">
        https://simran-be-my-valentine.com 💕
      </div>
    </div>

    <div class="content">
      <h1>💘 Simran, will you be my Valentine? 💘</h1>
      <p>Warning: Saying no is physically impossible 😌</p>

      <button class="yes" onclick="alert('YAY SIMRAN!!! 💖🥰💍')">
        Yes 💕
      </button>

      <button class="no" id="noBtn">
        No 😅
      </button>
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const browser = document.querySelector(".browser");

    noBtn.addEventListener("mouseenter", () => {
      const maxX = browser.clientWidth - noBtn.offsetWidth;
      const maxY = browser.clientHeight - noBtn.offsetHeight;

      const x = Math.random() * maxX;
      const y = Math.random() * maxY;

      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    });
  </script>
</body>
</html>
